#!/bin/sh

# Mac OS X: dot-command script to allow iteraplan to be launched in a terminal window from a double-click in the finder

chmod a+x  `dirname "$0"`/shutdown.sh

export JAVA_HOME=/Library/Java/Home

`dirname "$0"`/shutdown.sh
